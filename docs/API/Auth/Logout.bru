meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{authBaseUrl}}/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Accept: application/json
}
