%% Visão Geral - SUS-Connect APS MVP (Fase 05)

graph TD
    Coord["Coordenador de APS"]
    ACS["ACS / Equipe ESF"]

    Coord -->|"Consulta prioridades e cria ações"| API
    ACS -->|"Atualiza progresso agregado"| API

    API["REST API\nlocalhost:8205/api/v1"]

    API --> Service

    subgraph Service ["aps-prioritization-service"]
        Logic["Regras de negócio\n(Clean Architecture)"]
    end

    Service --> DB[("PostgreSQL\naps_priority_db")]

    Swagger["Swagger UI\n/swagger-ui"] -.-> API
    Health["Actuator Health\n/actuator/health"] -.-> Service
